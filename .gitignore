# Python
__pycache__/
*.pyc
venv/
desktop-bridge/venv/

# Environment / secrets — never commit
.env
*.env

# Local debug artifacts
_intent_check.py

# Per-PC bridge config — never tracked (each machine has its own keys)
desktop-bridge/run.bat

# One-shot operational scripts (may contain transient credentials)
rotate_livekit.py
rotate_livekit.bat
push_refactor_to_rog.py
